{
  "__template": true,
  "__comment": "{{system_name}} — OpenClaw safe starter config. No tokens, no device identity, no secrets.",
  "workspace": "{{home_root}}/.openclaw/workspace",
  "channels": {}
}
