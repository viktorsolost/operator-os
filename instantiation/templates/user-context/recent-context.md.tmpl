# Recent Context

Routing layer only. This file holds context a fresh operator needs quickly.

## Current system state

- Fresh install. Owner: {{owner_name}}. System: {{system_name}}.
- Vault: {{vault_location}}.
- Workspace: {{workspace_root}}.
- Runtimes: {{selected_runtimes_list}}.
- No prior session history.

## Open Items

(None — fresh instance)
