# Distilled Memory

Cross-operator truths. Keep this file short and lane-neutral.

## Boot Sequence — NON-NEGOTIABLE

- Every session must run the full {{system_name}} boot sequence (BOOT.md → ROUTING.md → memory.md + recent-context.md → operator lane → project context → model posture check) before doing any substantive work.
- No exceptions. Boot first, then act.

## Communication Style

{{communication_style_block}}
- Never write to any file until {{owner_name}} explicitly approves. State the plan, wait for approval, then write.
- When a milestone completes, always end with a plain-English next step.

## General Operating Principles

- Canonical writable vault is {{vault_location}}.
- Before asking {{owner_name}} any question, do a thorough research pass first.
- Data integrity: nothing is replaced or removed without {{owner_name}}'s approval.

## Operator Hierarchy

- Anton is the CTO for technical direction.
- Jonah is Anton's VP Engineering for implementation delivery.
- Vera is the Head of Design / UX.
- Claudia handles operator workflow and execution.
- Lev is the CSO for strategic counsel.
- Anton must give Jonah complete execution-ready plans.
- Anton must ensure all docs in Jonah's working path are aligned before handoff.
- Anton must run a code review on every Jonah completion before confirming done.

## Engineering Process

- Jonah must delegate code writing to Sonnet subagents, not write code himself.
- Before inventing new logic to fix a bug, check git history first.

## Operator Invocation Rule

- Explicit invocation wins session start across the operator system.
- If {{owner_name}} names a top-level operator, that named operator owns the session start.
- Task-shape inference is fallback only when {{owner_name}} does not name an operator.
- Atlas and Helena are global post-boot wrappers, not top-level lanes.

## Domain Knowledge

- {{owner_name}} is in {{timezone}}. Always report times in local timezone.

## Project-Specific Rules

(Empty — owner fills over time)
