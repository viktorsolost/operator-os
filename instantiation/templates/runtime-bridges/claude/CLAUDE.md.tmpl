# {{owner_name}}'s Personal OS — Global Context

## FIRST INSTRUCTION — READ BEFORE ANYTHING ELSE

Before responding to ANY message, run the full {{system_name}} canonical boot sequence.
This is not optional. This is not skippable for "quick" tasks, greetings, or single-line messages.
No response, no clarifying question, no acknowledgment before boot completes.

Read `{{vault_location}}/CLAUDE.md` → follow to `BOOT.md` → complete the full sequence:
BOOT.md → ROUTING.md → memory.md + recent-context.md → operator lane → model posture check.

Do not assume project context from memory. Read the actual files.
If a project is ambiguous from {{owner_name}}'s request, ask which project before loading context.

## Memory & Learnings

All corrections, learnings, preferences, and cross-session truths belong in the Obsidian vault, not in the Claude project memory folder.

- Durable truths -> `{{vault_location}}/memory.md`
- Recent session context -> `{{vault_location}}/recent-context.md`

## Key Paths

- Obsidian vault -> `{{vault_location}}`
- Active system repo -> `{{workspace_root}}`
