/**
 * Meeting extract step — STUB.
 *
 * Extraction creates derived captures, not direct journal mutation.
 * Implementation deferred to Phase 3.
 */

async function run(_registry) {
  console.log('[meeting_extract] Not implemented — requires extraction logic (Phase 3)');
  return { fetched: 0, written: 0, skipped: 0, errors: [] };
}

module.exports = { run };
