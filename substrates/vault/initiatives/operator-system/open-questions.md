# Operator System Open Questions

## Brand operators

- What is Atlas allowed to output by default: strategy memo, channel diagnosis, campaign plan, or all three?
- What is Helena allowed to output by default: draft, rewrite, review, rubric score, or all of them?
- What is the exact load order for a brand-aware session?
- When a brand pack is weak or contradictory, when should the agent proceed vs stop?
- What is the minimum approved doctrine required before Helena can draft in-brand confidently?

## Runtime and system evolution

- How much validation proof do we want per runtime before a routing change is considered fully shipped?
- Should runtime validation stay in one shared file or split by runtime as the system grows?
- How much operator-system context should be mirrored into initiative docs versus staying only in canonical operator files and runtime wrappers?
